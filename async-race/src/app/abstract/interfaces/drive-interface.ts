interface movingCarParams {
  carContainer: HTMLElement;
  id: string;
  car: HTMLElement;
  trackWidth: number;
  carWidth: number;
  ratio: number;
  stopButton: HTMLElement;
  goButton: HTMLElement;
}

export default movingCarParams;
