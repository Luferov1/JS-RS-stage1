enum ServerPath {
  address = 'http://127.0.0.1:3000',
  garage = '/garage',
  winners = '/winners',
  engine = '/engine',
  limit = '_limit=7',
  limitW = '_limit=10',
  page = '_page',
  sort = '_sort',
  order = '_order',
}

export default ServerPath;
