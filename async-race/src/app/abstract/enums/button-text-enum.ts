enum ButtonText {
  create = 'create',
  update = 'update',
  race = 'race',
  reset = 'reset',
  generateCars = 'generate cars',
  select = 'select',
  remove = 'remove',
  prev = 'prev',
  next = 'next',
}

export default ButtonText;
